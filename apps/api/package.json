{
  "name": "@rdx/api",
  "version": "0.0.0",
  "private": true,
  "description": "RDX backend API — chat agent and shared app endpoints",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@rdx/chat-contract": "*"
  },
  "devDependencies": {
    "@rdx/tsconfig": "*",
    "@types/node": "^24.13.3",
    "tsx": "^4.20.3",
    "typescript": "^5",
    "vitest": "^3.2.4"
  }
}
